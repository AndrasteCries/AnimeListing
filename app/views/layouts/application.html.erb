<!DOCTYPE html>
<html>
  <head>
    <title>AnimeListing</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "general", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="header">
    <%= link_to "AniNyaListing", "#default", class: "logo" %>
    <div class="header-right">
      <%= link_to "Main", root_path, class: "active" %>
      <%= link_to "Animes", root_path, class: "active" %>
      <% if user_signed_in? %>
        <%= link_to "Profile", root_path %>
      <% else %>
        <%= link_to "Log in", new_user_session_path %>
        <%= link_to "Sign in", new_user_registration_path %>
      <% end %>
    </div>
  </div>
  <div class="content">
    <%= yield %>
  </div>
  </body>
</html>
